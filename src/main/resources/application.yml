server:
  port: 8080
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        hbm2ddl:
          auto: update
    database: mysql
  datasource:
    url: jdbc:mysql://localhost:3306/movie
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

